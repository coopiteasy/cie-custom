<?xml version="1.0" encoding="utf-8" ?>
<odoo>
    <!-- the pos_loyalty Gift cards & eWallet menu should be available to pos users -->
    <record id="pos_loyalty.menu_gift_ewallet_type_config" model="ir.ui.menu">
        <field
            name="groups_id"
            eval="[
                Command.link(ref('point_of_sale.group_pos_user')),
            ]"
        />
    </record>
</odoo>
